#include "tokenizer.hpp"
#include "../tokens.hpp"
#include <optional>

// std::optional<Token> try_get_tok(std::string )

// std::optional<T

std::vector<Token> Tokenizer::tokenize(std::string input_str)
{
    std::vector<Token> toks;

    toks.push_back(Token::SEMICOLON_CH);

    return toks;
}